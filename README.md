# Homebrew tap for Rewrite Better

```bash
brew install --cask cvthang56th2/rewrite-better/rewrite-better
```

Upgrade later with `brew upgrade --cask rewrite-better`.

The cask installs the macOS menu bar app from the latest GitHub Release DMG. The first open can require a right-click → Open until the app is notarized.
